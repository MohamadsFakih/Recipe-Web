# Copy to .env (for Prisma CLI) and .env.local (for Next.js). Production: set these in DigitalOcean App Platform.

# -----------------------------------------------------------------------------
# Database (required)
# -----------------------------------------------------------------------------
# Local dev (SQLite):
DATABASE_URL="file:./dev.db"

# Production (DigitalOcean Managed PostgreSQL) – use the connection string from your DB:
# DATABASE_URL="postgresql://doadmin:YOUR_PASSWORD@YOUR_DB_HOST:25060/defaultdb?sslmode=require"

# -----------------------------------------------------------------------------
# Auth (required for login/sessions)
# -----------------------------------------------------------------------------
# Generate with: openssl rand -base64 32
AUTH_SECRET=your-secret-at-least-32-chars

# Production: set to your app URL (e.g. https://your-app.ondigitalocean.app)
# NEXTAUTH_URL=https://your-app.ondigitalocean.app

# -----------------------------------------------------------------------------
# AI recipe generation (optional)
# -----------------------------------------------------------------------------
# Get a token at https://huggingface.co/settings/tokens
# HUGGINGFACE_API_KEY=hf_xxxx
# HUGGINGFACE_MODEL=mistralai/Mistral-7B-Instruct-v0.2
