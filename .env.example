# Copy to .env and .env.local. Prisma reads .env; Next.js reads both.

# -----------------------------------------------------------------------------
# Database (required)
# -----------------------------------------------------------------------------
# Local dev: SQLite (no Docker or cloud DB needed)
DATABASE_URL="file:./dev.db"

# Production (Vercel etc.): set DATABASE_URL in the host dashboard to a Postgres URL.
# The build will auto-switch to Postgres when it sees postgresql:// in DATABASE_URL.

# -----------------------------------------------------------------------------
# Auth (required for login/sessions)
# -----------------------------------------------------------------------------
# Generate with: openssl rand -base64 32
AUTH_SECRET=your-secret-at-least-32-chars

# Production: set to your app URL (no trailing slash).
# Vercel: https://your-project.vercel.app
# NEXTAUTH_URL=https://your-project.vercel.app

# -----------------------------------------------------------------------------
# AI recipe generation (optional)
# -----------------------------------------------------------------------------
# Get a token at https://huggingface.co/settings/tokens
# HUGGINGFACE_API_KEY=hf_xxxx
# HUGGINGFACE_MODEL=mistralai/Mistral-7B-Instruct-v0.2

# -----------------------------------------------------------------------------
# Vercel Blob (production – for profile & recipe images to persist)
# -----------------------------------------------------------------------------
# On Vercel: Storage → Blob → connect to project. BLOB_READ_WRITE_TOKEN is set automatically.
# BLOB_READ_WRITE_TOKEN=
